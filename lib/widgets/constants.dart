import 'package:flutter/material.dart';

class AppConstants {
  // Warna utama aplikasi
  static const Color primaryColor = Color(0xFFEE4D2D);
  static const Color secondaryColor = Color(0xFFFFA500);
  static const Color backgroundColor = Color(0xFFF5F5F5);

  // Ukuran teks
  static const double titleFontSize = 18.0;
  static const double bodyFontSize = 14.0;

  // Konstanta lainnya
  static const String appName = "Shopee Clone";
  static const String defaultAvatarUrl = "https://via.placeholder.com/150";
}
